import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textInput: {
    width: 200,
    height: 40,
    textAlign: 'center',
    borderColor: 'gray',
    borderWidth: 1,
  },
});
